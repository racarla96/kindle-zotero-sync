# Referencia AGPL-3.0 (kosync.koplugin de KOReader) — consulta local
# únicamente, nunca se commitea a este repo CC BY 4.0. Ver CLAUDE.md §2.
example/

# Credenciales locales (nunca commitear)
config/config.json

# Descargas temporales del script de prueba
scripts/tmp/

# Build artifacts (fases futuras: meson/ninja)
build/
*.o
*.so

# Editores / SO
.vscode/
.idea/
*.swp
.DS_Store
