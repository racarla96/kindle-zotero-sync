local _ = require("gettext")
return {
    fullname = _("Zotero"),
    description = _([[Syncs your Zotero library (metadata via the Zotero Web API, PDF attachments via your own WebDAV) and lets you browse and open items with KOReader's native reader.]]),
}
